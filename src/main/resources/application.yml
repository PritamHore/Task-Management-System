server:
  port: 10001

spring:
  application:
    name: task-management
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/task-management-db}
    username: ${DB_USERNAME:pritam}
    password: ${DB_PASSWORD:pritamroot}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled : true